<div class=row>
  <% books.each do |book| %>
  <%= erb(:"_partials/book_card", {}, { book: book }) %>
  <% end %>
</div>
<footer class="book-footer">
  <%= erb(:"_partials/pagination", {}, {
      data: books,
      count: num_books,
      offset: offset,
      limit: limit, }) %>
</footer>
